<!DOCTYPE html>
<html>
<head>
	<title>Escola</title>
	<%= stylesheet_link_tag    'application', media: 'all' %>
	<%= csrf_meta_tags %>

	<%= yield(:css) %>

</head>
<body>

<div class="container">
	<% if notice.present? %>
	<div class="alert alert-<%= notice == 'Deu tilt' ? 'danger' : 'success' %>">
		<%= notice %>
	</div>
	<% end %>

	<%= yield %>
</div>

  <%= javascript_include_tag 'application' %>

  <%= yield(:js) %>

  <%= debug params %>
</body>
</html>
